{
  "name": "react-swap-animation",
  "version": "0.14.1",
  "description": "Swap animation for React",
  "main": "./lib/index.js",
  "style": "./lib/index.css",
  "scripts": {
    "clean": "rm -rf lib/",
    "build": "npm run build:js && npm run build:css",
    "build:js": "babel src/ --out-dir lib/",
    "build:css": "cp src/*.css lib/",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thomas Heymann <thom@totheskies.net> (http://totheskies.net/)",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/cyberthom/react-swap-animation"
  },
  "dependencies": {
    "classnames": "^2.2.5"
  },
  "peerDependencies": {
    "react": "^0.14.0 || ^15.0.0",
    "react-delegate-transition-group": "^0.14.1",
    "react-dom": "^0.14.0 || ^15.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-plugin-react-require": "^3.0.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0"
  }
}
